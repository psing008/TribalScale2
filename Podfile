# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'tribalScale2' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for tribalScale2

  
  target 'tribalScale2Tests' do
    inherit! :search_paths
    # Pods for testing
	pod 'OHHTTPStubs'
	
  end

  target 'tribalScale2UITests' do
    inherit! :search_paths
    # Pods for testing
	pod 'OHHTTPStubs'
  end

end
